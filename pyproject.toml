[tool.flet]
app_name = "SKUs app"
app_version = "1.1.0"
org = "com.tuempresa.catalogoproductos"

[tool.flet.build]
exclude = [
    "Data/*",
    "env/*",
    "*.xlsx",
    "*.env",
    ".env",
    "CLAUDE.md",
    "README.md",
    "requirements.txt",
    "upload_data.py"
]